# 个人博客设计文档

**日期: ** 2026-08-15  
**状态: ** 已确认, 待用户审阅后进入实施计划

## 1. 目标

从零构建一个面向中国大陆用户的个人表达型博客, 支持个人介绍、Markdown 文章、邮箱/OAuth 登录、点赞、评论审核、在线后台、数据库和可恢复的 GitHub 内容同步。

## 2. 已确认的产品范围

### 公开阅读端

- 首页展示个人介绍、最新文章和精选文章。
- 个人介绍页展示作者信息、兴趣和外部链接。
- 文章列表、文章详情、标签筛选和基础搜索。
- 文章详情显示发布日期、更新时间、标签、目录、阅读进度、点赞和评论。
- 支持 RSS、sitemap、SEO 元信息、响应式布局和暗色模式。
- 默认视觉方向参考 Josh Comeau: 深色沉浸式背景、蓝紫渐变强调色、大留白、强标题排版和轻量个性化视觉元素；不复制其插画、文案或品牌资产。

### 用户与互动

- 用户可以通过邮箱 + 密码注册和登录。
- GitHub/Google OAuth 作为可选登录方式, 不作为中国大陆用户的唯一登录入口。
- 登录用户可以点赞和发表评论。
- 同一用户对同一文章只能保留一个点赞关系。
- 评论默认进入 `pending` 状态, 审核通过后公开。
- 管理员可以审核、隐藏和删除评论。
- 第一版不做评论回复、举报、敏感词系统和邮件通知。

### 管理后台

- 只有管理员可以访问后台。
- 后台支持创建、编辑、保存和删除草稿。
- 草稿使用 Markdown 编辑器。
- 后台支持编辑标题、摘要、标签、封面、slug、发布时间和精选状态。
- 发布操作先保存国内数据库, 再异步同步到 GitHub。
- GitHub 同步失败时, 草稿不丢失, 状态变为待重试, 并支持自动重试和手动重试。

## 3. 页面路由

```text
/                       首页
/about                  个人介绍
/posts                  文章列表
/posts/[slug]           文章详情
/tags/[tag]             标签文章列表
/search                 搜索结果
/login                  登录与注册
/account                用户账户
/admin                  后台首页
/admin/posts            草稿和文章管理
/admin/posts/[id]       草稿编辑器
```

## 4. 技术架构

### 前端

- Astro + TypeScript 负责页面、Markdown 构建、SEO、RSS 和静态输出。
- Vue 3 负责登录表单、点赞按钮、评论区、搜索交互和后台编辑器。
- 使用 Astro islands, 只在需要状态和事件的组件上加载 Vue, 公开文章页面保持低客户端 JavaScript 负载。

### 生产后端

- Node.js + TypeScript 提供 API。
- Hono 作为轻量 API 框架。
- PostgreSQL 保存用户、评论、点赞、草稿和同步任务。
- Drizzle ORM 负责数据库访问和迁移。
- 国内云服务器或容器服务承载 API。
- 国内对象存储承载头像、封面和文章图片。

Supabase 可用于开发阶段快速验证认证和数据结构, 但不作为生产环境不可替换的核心依赖。前端只依赖项目自身 API, 便于后续迁移到国内数据库和服务。

### 内容来源

- 已发布文章的最终正文保存在 GitHub Markdown 文件中。
- 草稿、编辑状态、发布状态和同步状态保存在 PostgreSQL。
- 每篇文章使用稳定的 `contentId` 关联评论和点赞, slug 只负责 URL, 不作为互动数据的永久标识。

## 5. 核心数据模型

### Markdown frontmatter

```yaml
---
contentId: "stable-content-id"
title: "文章标题"
description: "文章摘要"
date: 2026-08-15
updatedAt: 2026-08-15
tags: ["随笔", "阅读"]
cover: "/images/example.jpg"
featured: false
draft: false
---
```

### 数据库表

- `profiles`: 用户昵称、头像、角色和创建时间。
- `post_drafts`: `contentId`、slug、标题、摘要、Markdown 正文、标签、封面、状态、Git 路径、最近 commit SHA、创建者和更新时间。
- `comments`: 文章 `contentId`、用户、正文、审核状态、创建时间和更新时间。
- `likes`: 用户与文章的关系, 建立 `(user_id, content_id)` 唯一约束。
- `sync_jobs`: GitHub 同步任务、重试次数、错误信息、下次重试时间和完成状态。

## 6. 内容发布与读取流程

### 发布流程

```text
管理员编辑草稿
  ↓
国内 API 保存数据库事务
  ↓
创建 GitHub 同步任务
  ↓
后台 worker 调用 GitHub API 生成或更新 Markdown
  ↓成功
记录 commit SHA, 触发构建部署
  ↓失败
记录错误, 指数退避重试, 保留手动重试入口
```

国内 API 到 GitHub 的跨境调用只影响发布速度, 不影响已经上线文章的阅读。发布任务必须幂等, 同一个 `contentId + revision` 不能生成重复内容提交。

### 公开读取流程

- Astro 构建阶段读取仓库 Markdown 并生成文章页面。
- 访客访问博客时只访问博客自己的域名, 不运行时读取 GitHub。
- 点赞和评论通过同域名 API 请求。
- 页面不引用 `raw.githubusercontent.com`、GitHub 图片、GitHub Pages 或外部字体作为关键资源。

## 7. 权限与安全

- 密码使用 Argon2id 哈希, 禁止保存明文密码。
- 登录状态使用 `HttpOnly`、`Secure`、`SameSite` Cookie。
- 管理员角色只能由服务端或受保护的数据库操作授予, 普通用户不能自行提升权限。
- 评论创建要求已登录；评论公开读取只返回审核通过的记录。
- 点赞接口使用数据库唯一约束并支持幂等请求。
- 登录、评论和点赞接口加入限流。
- GitHub Token、数据库密码和 OAuth 密钥只存储在服务端环境变量。
- 管理后台和发布接口在路由层、服务层和数据库层都校验管理员权限。

## 8. 中国大陆访问策略

- GitHub 作为代码和 Markdown 版本库, 不作为访客运行时依赖。
- 静态站点、API、数据库和图片存储部署在国内可用的基础设施上。
- 正式域名按实际部署方案完成 ICP 备案和相关合规配置。
- 邮箱密码登录作为国内用户的必需路径；海外 OAuth 只作为增强功能。
- 发布前测试首页、文章页、登录、评论、点赞和后台在中国大陆网络下的连通性。
- 通过服务接口和数据访问层隔离 Supabase/国内数据库实现, 避免前端组件与供应商 SDK 强绑定。

## 9. 错误处理

- Markdown frontmatter 校验失败时阻止构建, 并指出文件路径和具体字段。
- GitHub 同步失败时不删除或覆盖数据库草稿。
- 构建失败时保留上一版线上部署, 后台显示构建错误和 commit 信息。
- 评论提交失败时保留用户输入, 明确区分未登录、限流、审核中和服务器错误。
- 点赞重复请求返回当前点赞状态, 不显示为系统错误。
- API 不向客户端返回数据库密码、GitHub Token、内部堆栈和供应商密钥。

## 10. 测试与验收标准

### 自动化测试

- frontmatter 解析、字段校验和 Markdown 构建。
- 登录、退出、密码错误和会话过期。
- 普通用户与管理员的权限边界。
- 点赞幂等性和唯一约束。
- 评论创建、审核、隐藏和删除。
- 草稿保存、发布任务创建、失败重试和手动重试。
- GitHub 同步任务的幂等性。

### 集成与手工验收

- 从后台创建草稿并成功发布为 GitHub Markdown。
- GitHub 同步失败后, 草稿仍可见且可以重试。
- 构建部署后文章页面、RSS、sitemap 和 SEO 元信息正确。
- 未登录用户不能点赞、评论或访问后台。
- 国内网络环境可以完成首页浏览、文章阅读、邮箱登录、评论和点赞。
- 手机和桌面视口下导航、文章排版、评论区和编辑器可用。

## 11. 分阶段交付

### 阶段一: 公开博客

交付 Astro、Vue、Markdown 内容集合、首页、文章页、关于页、标签、搜索、主题切换、RSS、sitemap 和国内静态部署基础。

### 阶段二: 用户互动

交付国内 API、PostgreSQL、邮箱密码登录、会话管理、点赞、评论和管理员审核。

### 阶段三: 在线后台与 Git 发布

交付草稿编辑器、元信息管理、发布队列、GitHub Markdown 同步、重试机制、发布状态和完整验收测试。

## 12. 非目标

- 第一版不做多作者写作。
- 第一版不做评论回复、举报、敏感词平台和邮件订阅系统。
- 第一版不做复杂推荐算法、全文搜索服务和多语言内容管理。
- 第一版不把 GitHub、Vercel、普通 Cloudflare Pages 或 Supabase 作为中国大陆访问的必经运行时链路。
